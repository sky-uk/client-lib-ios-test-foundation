# Warning, do not change version.
version: 2.1

jobs:
  spm:
    macos:
      xcode: 12.3.0
    shell: /bin/bash --login -eo pipefail
    steps:
      - checkout
      - run:
          name: Enable cloning private Swift Package Manager repositories
          command: sudo defaults write com.apple.dt.Xcode IDEPackageSupportUseBuiltinSCM YES
      - run:
          name: Add GitHub to SSH Known Hosts
          command: |
            rm ~/.ssh/id_rsa || true
            for ip in $(dig @8.8.8.8 github.com +short); do ssh-keyscan github.com,$ip; ssh-keyscan $ip; done 2>/dev/null >> ~/.ssh/known_hosts || true
      - run:
         name: Build & Test SPM Package
         command: swift test
  carhage:
    macos:
      xcode: 12.3.0
    shell: /bin/bash --login -eo pipefail
    steps:
      - checkout
      - run:
          ./bootstrap
      - run:
          name: Add GitHub to SSH Known Hosts
          command: |
            rm ~/.ssh/id_rsa || true
            for ip in $(dig @8.8.8.8 github.com +short); do ssh-keyscan github.com,$ip; ssh-keyscan $ip; done 2>/dev/null >> ~/.ssh/known_hosts || true
      - run:
         name: Build & Test SPM Package
         command: xcodebuild test -disableAutomaticPackageResolution -scheme SkyTestFoundation -destination 'platform=iOS Simulator,name=iPhone 11' | xcpretty

workflows:
  version: 2
  build-test:
    jobs:
      - spm
      - carhage
